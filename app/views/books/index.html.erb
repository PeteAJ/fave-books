<p> Welcome <%= current_reader.name %>
<h2> These are Your Books: </h2>

<% book.each do |book| %>
  <div>
    <%= book.author %>
    <%= book.title %>
  </div>
  <% end %>

  <a href="/sessions/logout">Log Out</a>
